# HTTP port for local bun dev (default: 3100)
PORT=3100

# SQLite database path for local bun dev (default: agent-hub.db in project root)
DB_PATH=agent-hub.db

# Cloudflare account ID (for wrangler deploy)
# CLOUDFLARE_ACCOUNT_ID=
